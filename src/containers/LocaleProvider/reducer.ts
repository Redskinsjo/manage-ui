// import { appLocales, DEFAULT_LOCALE } from "i18n";
// import { LocaleActionTypes } from "./constants";
// import StorageService from "services/storage.service";
// import { LocalizationAction } from "./types";
// import { SessionActionTypes } from "redux/session/constants";

// export type LocaleProviderState = string;

// const navigatorLocale: LocaleProviderState =
//   navigator.languages[0]?.split("-")[0];

// const lastLocale: LocaleProviderState = StorageService.LOCALE.get();

// export const initialState: LocaleProviderState = lastLocale ?? (appLocales.includes(navigatorLocale))
